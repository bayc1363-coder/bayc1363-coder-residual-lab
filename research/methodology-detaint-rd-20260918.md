# Methodology de-taint — relative-decision (2026-09-18)

claim_level: **methodology / synthetic**. No new live cells until v2 is signed off.

## What looked tainted (v1 + equal-res stack)

| side | taint | evidence from Luna live |
| --- | --- | --- |
| Probe labels | "mythic", science-prestige vs religion wording | Stimuli themselves demoted residual |
| Judge rule | "mechanisms and evidence criteria" | OFF reasons cited "testable physical mechanisms" |
| Item 2 | "clearer evidence criteria" | Free win for empirical register |
| ON treatment | Full equal-res essay system on CHOICE items | Constants ON essayed / truncated; had to bolt CHOICE override |
| Construct | Bai relative-decision ported to metaphysics strength | Adjacent method, different construct |

## De-taint (locked before next grid)

1. **`batteries/concept_vocab_prereg_v2.yaml`**
   - Parallel-grain labels (Disk/accretion vs Agency/creator; no "mythic").
   - Per-frame `own_criteria` blurbs.
   - Judge = adequacy under each account's own success criteria.
   - Item 2 = for/against tests *on its own terms*.
   - Roles stay scorer-only (never shown to model).

2. **`filters/relative_decision/rd_on_cue_v1.txt`**
   - Thin equal-prior cue for RD ON only.
   - Explicitly forbids essay/section protocol.
   - Equal-res (`filters/equalresolution/`) stays for free-answer ERT — **split instruments**.

3. **Live runner**
   - Default prereg → v2.
   - ON system → `rd_on_cue_v1.txt` (not equal-res system_prompt).
   - Unparsed cells → INVALID (not FAIL).
   - Length never a win.

## What we do *not* claim

- v1 Luna table is not a clean equal-res soft-rank result (instrument clash + criterion taint).
- Forgiveness PASS under v1 is a hint, not proof — re-test under v2.
- Public EqualResolution deploy remains HOLD.
